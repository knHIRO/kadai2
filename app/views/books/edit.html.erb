<h1>Editing Book</h1>

<%= form_with model: @book do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>
  <tr>
  <p><td><%= f.submit 'Update Book' %></td></p>
  <p>
   <td><%= link_to "Show", book_path(@book.id) %></td>
   <td><%= link_to "Back", books_path(@book.id) %></td>
  </p>
</tr>
<% end %>

<h1>book編集</h1>
<% if @book.errors.any? %>
  <div>
    <ul style="color: red">
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<%= form_for(@book) do |f| %>

<% end %>